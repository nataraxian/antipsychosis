{
  "buildCommand": "pnpx convex deploy --cmd 'pnpm run build'",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
